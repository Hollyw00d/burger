<div class="container">
    <div class="row">
        <div class="col-lg-6">
            <ol>
            {{#each burgers}}
                <li>{{burger_name}}</li>
            {{/each}}
            </ol>
        </div>
    </div>
</div>